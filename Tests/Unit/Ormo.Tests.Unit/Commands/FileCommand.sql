CREATE TABLE IF NOT EXISTS table_from_file (
  id INTEGER PRIMARY KEY ASC, 
  value TEXT NOT NULL);

CREATE TABLE IF NOT EXISTS another_table_from_file (
  id INTEGER PRIMARY KEY ASC, 
  value TEXT NOT NULL);